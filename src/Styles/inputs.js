import { css } from "@emotion/core";
import { theme } from "../Styles/theme";

export const TextInput = css`
  border: none;
  color: ${theme.colors.grey200};
  border-bottom: 1px solid ${theme.colors.grey200};
  padding: ${theme.pm.pm100} 0;
  font-size: ${theme.text.text300};
  width: 100%;
  margin-bottom: ${theme.pm.pm300};
`;
export const InputRadioList = css`
  margin-bottom: ${theme.pm.pm200};
`;
export const RadioInput = css``;
